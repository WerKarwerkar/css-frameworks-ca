// import { sortPosts } from "../api/posts/index.mjs";
// import { renderPostTemplates } from "../templates/index.mjs";

// export function handleSortPosts(posts, sortType) {
//     let sortedPosts;
//     if (sortType === 'latest') {
//         sortedPosts = sortPosts(posts.slice(), 'latest');
//     } else if (sortType === 'oldest') {
//         sortedPosts = sortPosts(posts.slice(), 'oldest');
//     }
//     return sortedPosts;
// }